# 理想同学 Qwen 固定场景与 App 验收记录

验收时间：2026-08-18

底轮相关按钮和导航只做 Mock、参数、路由及异常分支验证，验收过程中没有发送实际移动或导航目标。摄像头、头部、投影和灯光按允许范围进行了实机验证。

## 结果

- HTML 结构校验：通过
- JavaScript 语法校验：通过
- 分时问候边界：10/10 通过
- Qwen 项目 Python 单元/异常分支回归：76/76 通过
- App 服务、地图编码和 Qwen 项目适配：14/14 通过
- 运动计数、ReID、防串人和视频编码专项：21/21 通过
- 本地 Skill 目录安全验证：29/29 通过；风扇按配置明确禁用
- Qwen 云端意图矩阵：30/30 通过，覆盖单 Skill、复合场景、偏离剧本和记忆操作
- Mock 指令：11 条全部正确下发
- 浏览器逐项点按覆盖：首页启动/停止、全部五个页签、地图选择/取消/导航、宠物与运动视频、前后左右/停止、开关灯、投食规格与确认/取消
- 指令覆盖：前进、后退、左转、右转、点控停止、任务急停、开灯、关灯、投食、地图点选导航、Qwen 端到端语音录音协议
- 状态覆盖：机器人在线、机器人离线、错误反馈、WebSocket 自动重连、实时坐标更新、地图加载、新视频推送
- Android：`testDebugUnitTest`、`lintDebug`、`assembleDebug` 全部通过
- Windows 构建机：前端契约、Capacitor 同步、`testDebugUnitTest`、`lintDebug`、`assembleDebug` 全部通过
- 生产依赖安全审计（`npm audit --omit=dev`）：0 个已知漏洞
- APK 包名：`com.lixiang.robot.companion.fixed`

## 实机链路

- App 中继实时状态：机器人在线，地图、位姿、程序六组件和 Qwen 任务状态均正常上报。
- 首页启动按钮：程序已运行时准确返回 `already_running`；状态和刷新按钮通过真实中继。
- 首页停止/启动按钮：真实中继闭环通过；停止后六组件均关闭，启动后六组件均恢复就绪。前后定位差约 1.04 cm，属于定位波动，未执行非零底盘速度。
- App 语音按钮：真实分片上传、机器人转码及 Qwen PCM 接收全部成功。
- 欢迎、会议、运动三类媒体各自使用独立资源；会议启动/暂停/继续、运动播放及关闭清理均成功。
- 投影关闭后头部闭环回到水平位，投影光源关闭，会议循环停止。
- 前摄环境感知实际调用 `gpt-5.6-luna` 成功；暗环境准确返回不适合投影/运动，没有误报成功。
- 俯卧撑历史录像复测准确得到 3 次，并依次生成“第一个、第二个、第三个”；检测到事件生成约 0.06–0.08 ms。
- 当前后摄无人分支准确返回 `no_exercise_person_detected`；没有虚报计数或完成。
- 运动结束底层事件和主会话都不再询问或播报上传视频；最终总数与“喝口水”关怀各播一次。
- 修复 Qwen `response.cancel` 完成竞态后，空闲取消不会再退出常驻服务或带停全部 Skill。
- App 会忽略导航生命周期切换时的瞬时 0×0 空地图，ROS 订阅线程保持运行。

APK：`release/ideal-robot-qwen-realtime-20260818.apk`

APK SHA-256：`88af5a5f4974c25e03b0685c9b2a24fde084ee1147e41d2883afa3384bc54ce2`
