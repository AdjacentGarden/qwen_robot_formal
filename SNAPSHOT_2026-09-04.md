# 2026-09-04 机器人源码备份

本次将机器人当前的两个代码目录保存到同一提交中：

| 仓库位置 | 机器人原位置 |
|---|---|
| 仓库根目录 | `/home/test/qwen_audio_3_realtime_flash_scenarios_resident_test` |
| `android_app/` | 上述正式项目的 `android_app/` |
| `reproduce/vendor/Car_real_copy/` | `/home/test/Car_real_copy` |

`Car_real_copy` 是当前实际工作树源码及地图快照，不是 `car_real_copy_zhenghang`。
它的本地 Git 基线为 `52348412d7dfa7ade879fbea67a76c0c1367d19c`，本快照包含该基线之后机器人工作树中已有的修改。
上传没有修改机器人上的源文件，没有启动、停止程序或操作任何硬件。

## 当前千问版本包含

- 启动时确认前后摄像头实际持续出帧；异常仅重启摄像头采集服务，首次加两次重试，持续失败则启动报错。
- 计数缓存播报裁除近数字静音，保留起声余量；命中缓存不依赖云端重连；保留播报顺序与打断逻辑。
- 当前场景、记忆、Skill、App 界面与桥接源码。

最近上述改动的局部验证：摄像头15项自动测试及3次机器人双摄采集通过；语音事件/客户端39项自动测试通过。它们不是本次对所有机器人功能重新做了实机验收。

## 恢复与限制

1. 在独立位置克隆本仓库，按上表恢复两个目录。不要在机器人仍运行时覆盖工作树。
2. `Car_real_copy` 只保存源码、配置与地图，不包含 `build/`、`install/`；必须在目标 ROS Humble 环境按其 README 构建。
3. 私有 `.env`、API Key、认证文件、个人长期记忆、人脸库、录音录像、运行日志、缓存和签名密钥不上传，需从机器人或自己的安全备份恢复。
4. `config/resident_profile.json` 是个人记忆配置，不在此次公开快照中。若需原有个性化和家庭/公司配置，单独恢复该文件及私有服务配置。
5. `robot_skills/environment_perception/run_legacy.sh` 的硬编码令牌只在公开副本中删除，改为读取已有 `MODELSCOPE_SDK_TOKEN` 环境变量；原机器人代码未改。
6. 本次不重新上传模型权重、视频、音乐或 APK。仓库原先已有的模型仍保留，但未对全部权重逐一验证是否与机器人一致；新模型/素材及外部系统服务依赖仍须按项目配置另外准备。因此这是一份源码备份，不是可直接刷机的一键系统镜像。
7. `reproduce/` 其他既有 vendor 和系统文件来自早期备份，保留供参考，不应假定全都已经更新到本次机器人状态。优先参考本文件；旧依赖锁中的校验值可能已过期。
8. App 的 `android_app/web/config.js` 与 Android 打包资源内对应配置中的认证令牌已替换为 `REPLACE_WITH_APP_AUTH_TOKEN`。重新打包/部署时需填入与自己的桥接服务一致的令牌；原机器人上的实际配置未改动。

具体文件校验值见 `reproduce/source_snapshot_20260904.json`。源码恢复后先进行无硬件检查，再由操作者安排实机测试。
