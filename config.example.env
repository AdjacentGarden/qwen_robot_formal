# 默认使用已经验证可用的北京公共地址，不需要填写 Workspace ID。
# 如果以后改用业务空间专属地址，再取消下一行注释并填入真实值。
# DASHSCOPE_WORKSPACE_ID='替换为正确的北京地域WorkspaceId'

# 推荐把 Key 单独放到 runtime/api_key 并 chmod 600，不要写在这里。
# DASHSCOPE_API_KEY='sk-xxxxxxxx'
