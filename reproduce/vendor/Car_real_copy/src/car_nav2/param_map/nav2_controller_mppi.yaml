controller_server:
  ros__parameters:
    use_sim_time: true
    controller_frequency: 12.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    failure_tolerance: 0.3
    progress_checker_plugin: progress_checker
    goal_checker_plugins: [general_goal_checker]
    controller_plugins: [FollowPath]

    progress_checker:
      plugin: nav2_controller::SimpleProgressChecker
      required_movement_radius: 0.05
      movement_time_allowance: 10.0
    general_goal_checker:
      plugin: nav2_controller::SimpleGoalChecker
      stateful: true
      xy_goal_tolerance: 0.35
      yaw_goal_tolerance: 0.30

    FollowPath:
      plugin: nav2_mppi_controller::MPPIController
      motion_model: DiffDrive
      diff_drive:
        plugin: mppi::DiffDriveMotionModel
      time_steps: 40
      model_dt: 0.05
      batch_size: 400
      iteration_count: 1
      vx_std: 0.15
      vy_std: 0.0
      wz_std: 0.35
      vx_max: 0.4
      vx_min: -0.05
      vy_max: 0.0
      wz_max: 0.4
      ax_max: 0.4
      ax_min: -0.4
      ay_max: 0.0
      ay_min: 0.0
      az_max: 0.4
      prune_distance: 2.0
      transform_tolerance: 1.0
      temperature: 0.3
      gamma: 0.015
      visualize: false
      retry_attempt_limit: 1
      regenerate_noises: false
      TrajectoryVisualizer:
        trajectory_step: 5
        time_step: 3
      critics: [ConstraintCritic, CostCritic, GoalCritic, GoalAngleCritic, PathFollowCritic, PreferForwardCritic]
      ConstraintCritic:
        enabled: true
        cost_power: 1
        cost_weight: 4.0
      CostCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.81
        critical_cost: 300.0
        # consider_footprint: true
        collision_cost: 1000000.0
        near_goal_distance: 0.6
        inflation_layer_name: inflation_layer
      GoalCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 1.4
      GoalAngleCritic:
        enabled: true
        cost_power: 1
        cost_weight: 3.0
        threshold_to_consider: 0.5
      PathFollowCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        offset_from_furthest: 5
        threshold_to_consider: 1.4
      PreferForwardCritic:
        enabled: true
        cost_power: 1
        cost_weight: 5.0
        threshold_to_consider: 0.5
